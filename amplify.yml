version: 0.1
backend:
  phases:
    build:
      commands:
        - '# Execute Amplify CLI with the helper script'
        - '#amplifyPush --simple'
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
    build:
      commands:
        - PUBLIC_URL=https://assets-staging.causeandcuisine.com/build/ yarn run build
    postBuild:
        commands:
        - aws s3 rm s3://cause-cuisine-site-assets/ --recursive
        - yarn run upload-assets   
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
