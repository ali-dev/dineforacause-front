version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - PUBLIC_URL=https://assets-staging.causeandcuisine.com/build/ npm run build
    postBuild:
        commands:
        - aws s3 rm s3://cause-cuisine-site-assets/ --recursive
        - npm run upload-assets   
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
